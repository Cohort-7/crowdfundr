%h1= @category.name
- @category.projects.each do |project|
	%p
		%b= link_to(project.title, project)
	%p= project.description
	%p
		%b Goal:
		$#{project.goal}
	%p
		%b Status:
		= time_left_or_passed(project.end_time)
	=image_tag("http://www.htxt.co.za/wp-content/uploads/2013/10/gumstick.jpg")
